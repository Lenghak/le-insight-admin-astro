---
import { H1 } from "@/common/components/ui/h1";
import { BackButton } from "@/modules/error/components/back-button";

import { Muted } from "@/common/components/ui/muted";
import RootLayout from "@/common/layouts/root-layout.astro";
---

<RootLayout title="Le-Insight | 404">
  <main class="flex h-screen flex-col items-center justify-center space-y-8">
    <H1 className="text-7xl underline lg:text-9xl">404</H1>
    <Muted className="max-w-sm text-center">
      Not Found! Looks like you have stumble across a page that does not exist
      in our universe.
    </Muted>

    <div class="mt-8 flex flex-row items-center justify-center gap-4">
      <BackButton client:load />
    </div>
  </main>
</RootLayout>
